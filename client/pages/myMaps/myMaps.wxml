
<view class='tip'>左滑可删除</view>
<delete-list config-list="{{list}}"  bind:deleteItem="onDeleteItem" >
    <view wx:for='{{mapList}}'  wx:key='mapid' slot='{{item.mapid}}'>
        <map-item item-detail="{{item}}"/>
    </view>
</delete-list>
